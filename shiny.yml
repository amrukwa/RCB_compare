version: 1.1
kind: component
name: rcb-compare
run:
  kind: service
  ports:
    - 3838
  rewritePath: true
  container:
    image: amrukwa/rcb-compare
    env:
      - name: APPLICATION_LOGS_TO_STDOUT
        value: 'false'  
    